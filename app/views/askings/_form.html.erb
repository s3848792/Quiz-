<%= form_for(asking) do |f| %>
  <% if asking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asking.errors.count, "error") %> prohibited this asking from being saved:</h2>

      <ul>
      <% asking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %>
    <%= f.text_field :question %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :multiple_correct_answers %>
    <%= f.text_field :multiple_correct_answers %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
